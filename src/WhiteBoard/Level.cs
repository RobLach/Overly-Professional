using System;
using System.Threading;
using System.Diagnostics;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Content;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;
using Microsoft.Xna.Framework.Net;
using WhiteBoard.Controller;
using WhiteBoard.Objects;
using WhiteBoard.Collisions;
using Microsoft.Xna.Framework.Audio;

namespace WhiteBoard
{
    class Level
    {
        /*
        Vector2 position;
        public Vector2 Position
        {
            get { return position; }
            set { position = value; }
        }

    }

    public class LevelContentReader : ContentTypeReader<Level>
    {
        protected override Level Read(
            ContentReader input,
            Level existingInstance)
        {
            Level level = new Level();
            level.Position = input.ReadVector2();
            level.load(input.ContentManager);

            return level;
        }*/
    }

}
